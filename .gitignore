# Node / build (maqueta-react)
node_modules/
dist/
.vite/

# OS
.DS_Store
Thumbs.db

# credenciales del servidor: nunca al repositorio
.env
app/.env

# copias de seguridad: van al disco externo y a la nube, NUNCA al repositorio.
# Están cifradas, pero el repositorio es público y son datos de pacientes.
backups/
*.sql.enc
